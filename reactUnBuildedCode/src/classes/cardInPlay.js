export class CardInPlay
{
    constructor(card,numberOfColumn,team,lineCompatibility)
    {
        this.card = card;
        this.numberOfColumn = numberOfColumn;
        this.team = team;
        this.lineCompatibility = lineCompatibility;
    }
}